# DeepCoderX Essential Requirements - Python 3.9 Compatible

# Core dependencies that are actually needed for startup
python-dotenv>=1.0.0
rich>=13.0.0
requests>=2.31.0
openai>=1.0.0
pyyaml>=6.0.0
pygments>=2.17.0
pyperclip>=1.8.0

# For GGUF model support
llama-cpp-python>=0.2.0

# macOS specific (for readline support)
gnureadline; sys_platform == 'darwin'

# Testing (optional)
pytest>=7.4.0
pytest-mock>=3.11.0
